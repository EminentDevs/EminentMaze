namespace WPF_Client
{
	internal enum UIscales : byte { _80, _100, _120, _140}
	internal enum Panels : byte { NONE, CREATE, EDIT, SOLUTION, RUN_THE_MAZE, SETTINGS, ABOUT}
	internal enum Hotkeys : byte { NONE, BLOCK, EMPTY, START, FINISH }
	internal enum SolveMethod : byte { MOUSE, FLUID }
	internal enum DisplayMethod : byte { SOLUTION, PROCEDURE }
}
